<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <!-- Start Point EditText -->
    <EditText
        android:id="@+id/startPointEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Start Point (lat,lng)"
        android:inputType="text"
        android:minHeight="48dp" />

    <!-- Destination Point EditText -->
    <EditText
        android:id="@+id/destinationPointEditText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Enter Destination Point (lat,lng)"
        android:inputType="text"
        android:minHeight="48dp" />

    <!-- Transport Mode Dropdown -->
    <Spinner
        android:id="@+id/transportModeSpinner"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:entries="@array/transport_modes"
        android:minHeight="48dp" />

    <!-- Button to calculate the route -->
    <Button
        android:id="@+id/calculateRouteButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Calculate Route" />

    <!-- TextView to display the calculated route -->
    <TextView
        android:id="@+id/routeTextView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="20dp"
        android:text="Route details will appear here"
        android:textSize="16sp" />
</LinearLayout>
